name: Problem Detector Plugin
version: 1.0.0
description: Detects and reports problems found in the site.

form:
  fields:
    enabled:
      type: toggle
      label: Plugin status
      highlight: 1
      default: 0
      options:
        1: Enabled
        0: Disabled
      validate:
        type: bool
